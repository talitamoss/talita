<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".TalitaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Talita">

        <!-- MAIN ACTIVITIES -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Talita">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DATA COLLECTION ACTIVITIES -->
        <activity android:name=".LocationActivity" />
        <activity android:name=".AudioActivity" />
        <activity android:name=".QuickAddActivity" 
            android:theme="@style/Theme.Talita.Translucent" />
        <activity android:name=".QuickAddTestActivity" />
        
        <!-- DATA MANAGEMENT ACTIVITIES -->
        <activity android:name=".DataSummaryActivity" />
        <activity android:name=".DataViewActivity" />
        <activity android:name=".DataTypeDetailActivity" />
        <activity android:name=".MyDataActivity"
            android:theme="@style/Theme.Talita" />
        
        <!-- PATTERN & VISUALIZATION ACTIVITIES -->
        <activity android:name=".PatternsActivity" />
        <activity android:name=".NowViewActivity" 
            android:theme="@style/Theme.Talita.Fullscreen" />
        
        <!-- VAULT & EXPORT ACTIVITIES -->
        <activity android:name=".VaultActivity" />
        
        <!-- SETTINGS ACTIVITIES -->
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AboutSettingsActivity" />
        <activity android:name=".BackgroundTrackingSettingsActivity" />
        <activity android:name=".CloudBackupSettingsActivity" />
        <activity android:name=".DataCollectorsSettingsActivity" />
        <activity android:name=".DataExportSettingsActivity" />
        <activity android:name=".SecurityPrivacySettingsActivity" />
        <activity android:name=".EnhancedSettingsActivity" />
        <activity android:name=".TileSelectionActivity" />
        
        <!-- PLUGIN ACTIVITIES -->
        <activity android:name=".PluginManagementActivity"
            android:theme="@style/Theme.Talita" />
        <activity android:name=".LogActivity"
            android:theme="@style/Theme.Talita.Translucent" />

        <!-- SERVICES -->
        <service android:name=".BackgroundTrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- RECEIVERS -->
        <receiver android:name=".ActivityRecognitionReceiver"
            android:exported="false" />

        <!-- FILE PROVIDER -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
